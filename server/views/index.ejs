<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <title>Feature Flag</title>

    <link rel="stylesheet" href="/main.css">
    <script src="https://cdn.jsdelivr.net/npm/dayjs@1/dayjs.min.js"></script>
    <script>
        window.flagsOwn = `<%= JSON.stringify(flagsOwn) %>`;
        window.flagsSingle = `<%= JSON.stringify(flagsSingle) %>`;
        window.dayjs = dayjs;
    </script>
</head>
<body class="container">
<main class='stack stack-gap-large'>
    <h1>&nbsp;&nbsp;Feature Flags&nbsp;&nbsp;</h1>

    <div>
        <h6>Options for how DBs are set up</h6>
        <div id='radio-group' class='stack stack-gap-small'>
            <label for='radio-button-own'>
                <input
                    type='radio'
                    id='radio-button-own'
                    name='radio-group'
                    value='own'
                    onchange='ownDb.reset(); ownDb.setup()'
                />
                Each environment has its own DB
            </label>

            <label for='radio-button-single'>
                <input
                    type='radio'
                    id='radio-button-single'
                    name='radio-group'
                    value='single'
                    onchange='singleDb.reset(); singleDb.setup()'
                />
                Each environment uses the same DB
            </label>
        </div>
        <hr/>
    </div>

    <div id='content'></div>
</main>

<script src='/own-db.js'></script>
<script src='/single-db.js'></script>
</body>
</html>